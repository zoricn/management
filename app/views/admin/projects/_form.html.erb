<%= semantic_form_for([:admin, @project]) do |f| %>

    <%= f.inputs :name => "Basic" do %>
      <%= f.input :name, :required => true %>
      <%= f.input :description %>
    <% end %>

    <%= f.buttons %>
<% end %>
